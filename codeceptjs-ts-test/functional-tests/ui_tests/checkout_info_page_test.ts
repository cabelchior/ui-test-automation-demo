xFeature('Checkout info page'); //, { retryBefore: 2 });

/*** Feature Global Variables ***/

/*** Test Cases ***/

Before(({ I }) => {
  // do something...
});

Scenario.todo('Test something... @desktop', async ({ I }) => {
  // pause();
});

// User attempts to continue checkout without filling in a required field
// User continues the checkout after filling in all required fields
// User cancels checkout and navigates back to the Cart
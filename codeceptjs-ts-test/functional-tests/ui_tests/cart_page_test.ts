xFeature('Cart page'); //, { retryBefore: 2 });

/*** Feature Global Variables ***/

/*** Test Cases ***/

Before(({ I }) => {
  // do something...
});

Scenario.todo('User  @desktop', async ({ I }) => {
  // pause();
});

// User navigates from Cart back to PLP
// User navigates from Cart to Checkout page
// User removes items from Cart
// For a given product, the info in Cart corresponds to the info in PLP
xFeature('Checkout overview and finish pages'); //, { retryBefore: 2 });

/*** Feature Global Variables ***/

/*** Test Cases ***/

Before(({ I }) => {
  // do something...
});

Scenario.todo('Test something... @desktop', async ({ I }) => {
  // pause();
});

// For a each product, the info in the Overview page corresponds to the info in Cart
// User sees the order summary with the correct info
// User cancels checkout and navigates back to the PLP
// User finishes the checkout and navigates to the 'Checkout Complete' page